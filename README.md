Docker image for [ChuckTSI's Better Than Nothing Web Interface](https://github.com/ChuckTSI/BetterThanNothingWebInterface)


# Usage
```
docker-compose build
docker-compose up -d
```

You should now be able to access the page at localhost:6500
